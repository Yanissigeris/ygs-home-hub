
## What's Already Done ✅
- **Sitemap.xml** with 196 URLs + xhtml:link hreflang (fr-CA, en-CA, x-default)
- **robots.txt** with Disallow rules for thank-you pages
- **Canonical tags** — self-referencing on every page via PageMeta
- **Hreflang tags** — fr-CA / en-CA on all 80+ bilingual route pairs
- **JSON-LD schemas** — RealEstateAgent, Person, WebSite (with SearchAction), BreadcrumbList, Service, FAQPage, LocalBusiness (with openingHours, geo coords)
- **Open Graph + Twitter Card** metadata on every page
- **Visible breadcrumbs** on all internal pages
- **Language switcher** — visible, no forced redirects, both versions crawlable
- **FAQ sections with schema** on FAQ page + neighborhood pages
- **68+ internal contextual links** across blog ↔ neighborhoods ↔ funnels
- **Mobile-optimized**, lazy-loaded sections, optimized hero

## ⚠️ Critical Decision: `/fr/` Prefix
Your brief requests French pages under `/fr/`. Currently French is at root (`/`). **Moving to `/fr/` would break every indexed URL** and require 80+ redirects. This is a destructive change. **Recommendation: Keep current structure** (root = FR, `/en/` = EN) — this is a valid, Google-approved pattern used by major bilingual sites.

## Proposed Surgical Improvements (Priority Order)

### Phase 1: On-Page SEO Polish
1. **Audit heading structure** (H1/H2/H3) on top 10 pages — fix any missing H1s or duplicate H1s
2. **Add image alt text** across all pages (hero images, portraits, neighborhood photos)
3. **Homepage: add FAQ teaser** — 3-4 top questions with link to full FAQ page
4. **Homepage: add internal links section** to top location/service pages (Aylmer, Hull, Plateau, Chelsea, Cantley)

### Phase 2: Missing High-Value Pages
Most requested pages already exist under different URLs:
- `/vendre-ma-maison-gatineau` ≈ "vendre maison Gatineau" ✅
- `/evaluation-gratuite-gatineau` ≈ "évaluation maison Gatineau" ✅
- `/aylmer`, `/hull`, `/plateau` ≈ location pages ✅
- `/en/sell`, `/en/home-valuation`, `/en/aylmer`, `/en/hull` ✅

**No new pages needed** — existing pages cover all target keywords. We optimize existing ones instead.

### Phase 3: Internal Linking Boost
5. **Add contextual links** from homepage to Chelsea, Cantley, Val-des-Monts pages
6. **Cross-link service pages** to valuation/contact more consistently
7. **Add "related areas" links** on each neighborhood page

### Phase 4: Content & Copy Polish  
8. **Tighten meta descriptions** for CTR on remaining pages not yet audited
9. **Ensure natural keyword placement** in H1/H2s for target terms (courtier immobilier Outaouais, bilingual realtor Gatineau, etc.)

### NOT Doing (per your preservation rules)
- ❌ No visual redesign
- ❌ No `/fr/` URL restructure
- ❌ No new pages (existing ones cover all targets)
- ❌ No branding changes
- ❌ No layout changes
