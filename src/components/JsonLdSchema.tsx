/**
 * Previously injected review JSON-LD at runtime.
 * Review schema has been intentionally removed to avoid
 * Search Console "Multiple reviews without aggregateRating" warnings.
 * The static RealEstateAgent schema in index.html covers business info.
 */
const JsonLdSchema = () => null;

export default JsonLdSchema;
